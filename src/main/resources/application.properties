spring.application.name=authentication
jwt.secret=E52A41C54318944A7A23BF2E52EC8

# OAuth2 Google Configuration
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET
spring.security.oauth2.client.registration.google.scope=openid,email,profile
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/oauth2/callback/{registrationId}

# OAuth2 GitHub Configuration (Optional Example)
spring.security.oauth2.client.registration.github.client-id=YOUR_GITHUB_CLIENT_ID
spring.security.oauth2.client.registration.github.client-secret=YOUR_GITHUB_CLIENT_SECRET
# The default redirect URI and provider details are often automatically configured
app.frontend.url=http://localhost:3000
# --- JDBC MySQL Database Configuration ---
spring.datasource.url=jdbc:mysql://localhost:3306/authentication_manager_db
spring.datasource.username=auth_manager_user
spring.datasource.password=B@na9aE@te1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA/Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Spring Mail/Email Configuration ---

# Server Host (e.g., for Gmail)
spring.mail.host=smtp.gmail.com
# Server Port (e.g., 587 for TLS)
spring.mail.port=587

# Your Email Address
spring.mail.username=YOUR_EMAIL_ADDRESS
# Your App Password (mandatory for Gmail, not your login password)
spring.mail.password=YOUR_APP_PASSWORD

# Enable properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# The email address that appears as the sender
app.email.sender=YOUR_EMAIL_ADDRESS